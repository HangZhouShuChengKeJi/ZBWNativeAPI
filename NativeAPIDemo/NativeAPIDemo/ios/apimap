pop()->()~NativeAbility
alert(title,message,cancelButtonTitle,confirmButtonTitle)->(clickIndex)~NativeAbility
push(code,{})->()~NativeAbility
toRoot(tabIndex)->()~NativeAbility
openUrl(scheme)->()~NativeAbility
canOpenUrl(scheme)->(status)~NativeAbility
webTitle(title)->()~NativeAbility
toast(msg)->()~NativeAbility
shakeServicesEnable()->(status)~NativeAbility
shakeServicesDisable()->(status)~NativeAbility
camera()->(binaryEncode)~NativeAbility
screenshot(cutSizeWidth,cutSizeHeight,scale)->(binaryEncode)~NativeAbility
location()->(lat,lng)~AppDelegate
login()->(userId,userNick,userAvatar)~NativeAbility
scheduleNotification(time,title,content)->(status)~NativeAbility
alipay(orderNo,productName,productDescription,amount,notifyUrl,scheme)->(orderNo,status)~NativeAbility
share(shareUrl,shareTitle,shareImageUrl,shareDescription,showFlag)->(channel,status)~NativeAbility
loadImage(imageUrl)->()~NativeAbility

